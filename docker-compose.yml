# services:
#   file-ingestion-service:
#     image: ghcr.io/arshavin023/lamisplus_ingestion/file-ingestion-service:latest
#     build:
#       context: .
#       dockerfile: ./records_ingestion/Dockerfile
#     env_file:
#       - .env
#     environment:
#       - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     volumes:
#       - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
#       - ./records_ingestion/logs:/app/logs
#     restart: "no"
#     command: python records_ingestion/multi_file_ingestion_process.py

#   dsd-ingestion-service:
#     image: ghcr.io/arshavin023/lamisplus_ingestion/dsd-ingestion-service:latest
#     build:
#       context: .
#       dockerfile: ./records_ingestion/Dockerfile 
#     env_file:
#       - .env
#     environment:
#       - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     volumes:
#       - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
#       - ./records_ingestion/logs:/app/logs
#     restart: "no"
#     command: python records_ingestion/multi_dsd_ingestion_process.py
  
#   records-deletion-service:
#     image: ghcr.io/arshavin023/lamisplus_ingestion/records-deletion-service:latest
#     build:
#       context: .
#       dockerfile: ./records_deletion/Dockerfile
#     env_file:
#       - .env
#     environment:
#       - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     volumes:
#       - ./records_deletion/logs:/app/logs
#     restart: "no"

#   file-deletion-service:
#     image: ghcr.io/arshavin023/lamisplus_ingestion/file-deletion-service:latest
#     build:
#       context: .
#       dockerfile: ./file_deletion/Dockerfile
#     env_file:
#       - .env
#     environment:
#       - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     volumes:
#       - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
#       - ./file_deletion/logs:/app/logs
#     restart: "no"


services:
  # The unified single image build target
  file-ingestion-service:
    image: ghcr.io/arshavin023/lamisplus_ingestion/records-ingestion:latest
    build:
      context: .
      dockerfile: ./records_ingestion/Dockerfile
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
      - ./records_ingestion/logs:/app/logs
    restart: "no"
    # This acts as the default command if none is provided
    command: python records_ingestion/multi_file_ingestion_process.py

  # This service completely inherits the exact image built above, bypassing a second build step!
  dsd-ingestion-service:
    image: ghcr.io/arshavin023/lamisplus_ingestion/records-ingestion:latest
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
      - ./records_ingestion/logs:/app/logs
    restart: "no"
    # Simply override the script execution at runtime
    command: python records_ingestion/multi_dsd_ingestion_process.py
  
  records-deletion-service:
    image: ghcr.io/arshavin023/lamisplus_ingestion/records-deletion-service:latest
    build:
      context: .
      dockerfile: ./records_deletion/Dockerfile
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./records_deletion/logs:/app/logs
    restart: "no"

  file-deletion-service:
    image: ghcr.io/arshavin023/lamisplus_ingestion/file-deletion-service:latest
    build:
      context: .
      dockerfile: ./file_deletion/Dockerfile
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${FILE_DIRECTORY}:${FILE_DIRECTORY}
      - ./file_deletion/logs:/app/logs
    restart: "no"